# 🚀 가격표 자동 업데이트 가이드

## 📋 개요

엑셀 파일을 업로드하면 자동으로 CSV로 변환하고 사이트에 배포하는 도구입니다.

---

## 🎯 이전 vs 지금

### 이전 방식 (수동) 😓
1. 엑셀에서 CSV_출력 영역 복사
2. 메모장에 붙여넣기
3. CSV로 저장
4. 인코딩 확인
5. 파일명 변경
6. Git add
7. Git commit
8. Git push

**총 소요 시간: 5-10분**

### 지금 방식 (자동) 🚀
1. 엑셀 파일 업로드
2. 스크립트 실행 1번

**총 소요 시간: 10초**

---

## 📁 파일 설명

### 1. `update_prices.py` - 변환만
엑셀을 CSV로 변환만 하고, 배포는 수동으로 하고 싶을 때

### 2. `auto_deploy.py` - 변환 + 배포 (추천!)
엑셀 변환부터 Git 배포까지 완전 자동

---

## 🔧 사용 방법

### 방법 1: 가장 간단 (추천!)

```bash
# 1. 엑셀 파일을 /home/user/uploaded_files/ 에 업로드
# 2. 스크립트 실행
cd /home/user/webapp
python3 auto_deploy.py
```

스크립트가 자동으로:
- 최신 엑셀 파일 찾기
- CSV로 변환
- Git 커밋 & 푸시
- 배포 완료!

### 방법 2: 파일 지정

```bash
cd /home/user/webapp
python3 auto_deploy.py /path/to/your/excel.xlsx
```

### 방법 3: 변환만 (배포는 나중에)

```bash
cd /home/user/webapp
python3 update_prices.py
```

---

## 📊 스크립트가 하는 일

### 1. 엑셀 파일 찾기
- `uploaded_files` 폴더에서 가장 최근 `.xlsx` 파일 자동 검색
- 또는 직접 경로 지정 가능

### 2. 데이터 추출
- "CSV_출력(손대지마)" 컬럼부터 끝까지 추출
- 첫 행을 헤더로 사용
- 빈 행 자동 제거

### 3. CSV 저장
- UTF-8 (BOM) 인코딩으로 저장
- 한글 깨짐 방지
- 기존 `data.csv` 백업 (타임스탬프 포함)

### 4. Git 배포 (auto_deploy.py만)
- 변경사항 스테이징
- 타임스탬프 포함 커밋 메시지 자동 생성
- 원격 저장소와 동기화
- 자동 푸시

---

## 💡 예시 시나리오

### 시나리오 1: 아침에 가격 업데이트
```bash
# 1. 새 엑셀 파일 업로드
# 2. 한 줄 실행
python3 auto_deploy.py

# 결과: 1분 후 사이트에 반영 ✅
```

### 시나리오 2: 여러 번 테스트
```bash
# 변환만 (배포 안함)
python3 update_prices.py

# 브라우저에서 확인
# Ctrl + F5 (강력 새로고침)

# 문제없으면 수동 배포
git add data.csv
git commit -m "update: 가격표 업데이트"
git push origin main
```

### 시나리오 3: 특정 파일 사용
```bash
python3 auto_deploy.py ~/Downloads/오늘가격표.xlsx
```

---

## 🎨 출력 예시

```
============================================================
                       🚀 가격표 자동 업데이트                        
============================================================

ℹ️  최신 엑셀 파일 검색 중...
✅ 발견: 전기종특가_연동출력_요금제포함_요금할인추가_스크롤수정.xlsx
✅ 기존 파일 백업: data.backup.20260212_021347.csv

============================================================
                      📊 엑셀 → CSV 변환                      
============================================================

ℹ️  입력: 전기종특가_연동출력_요금제포함_요금할인추가_스크롤수정.xlsx
ℹ️  출력: data.csv
✅ CSV_출력 컬럼 발견 (인덱스: 32)
✅ 변환 완료: 10행 x 15열

============================================================
                           🚀 Git 배포                           
============================================================

ℹ️  변경사항 커밋...
✅ 변경사항 커밋
ℹ️  원격 저장소에 푸시...
✅ 원격 저장소에 푸시
✅ 배포 완료!

============================================================
                           ✅ 완료!                            
============================================================

✅ 가격표가 성공적으로 배포되었습니다!

약 1-2분 후 사이트에 반영됩니다:
  👉 https://a2272a.github.io/

새로고침: Ctrl+F5 (강력 새로고침)
```

---

## 🔒 안전 기능

### 1. 백업 자동 생성
- 변환 전 기존 `data.csv` 자동 백업
- 파일명: `data.backup.YYYYMMDD_HHMMSS.csv`
- 언제든 이전 버전으로 복구 가능

### 2. 미리보기
- 변환 후 처음 3행 자동 표시
- 데이터 확인 후 배포 진행

### 3. 확인 프롬프트
- `auto_deploy.py`는 배포 전 확인 요청
- Ctrl+C로 언제든 취소 가능

---

## 🚨 문제 해결

### 문제: "엑셀 파일을 찾을 수 없습니다"
**해결:**
```bash
# 파일 직접 지정
python3 auto_deploy.py /home/user/uploaded_files/파일명.xlsx

# 또는 파일 업로드 확인
ls -la /home/user/uploaded_files/
```

### 문제: "CSV_출력 컬럼 없음"
**해결:**
- 엑셀에 "CSV_출력(손대지마)" 컬럼이 있는지 확인
- 없으면 전체 데이터가 사용됨 (경고 표시)

### 문제: "Git 배포 실패"
**해결:**
```bash
# 수동 배포
cd /home/user/webapp
git add data.csv
git commit -m "update: 가격표 업데이트"
git push origin main
```

### 문제: "한글 깨짐"
**해결:**
- 스크립트는 UTF-8 (BOM) 사용
- 이미 깨짐 방지 처리됨
- 브라우저 강력 새로고침: Ctrl+F5

---

## 📱 모바일에서 확인

1. **사이트 접속**
   - https://a2272a.github.io/

2. **새로고침**
   - iOS Safari: 새로고침 버튼 길게 누르기
   - Android Chrome: 메뉴 → 새로고침

3. **캐시 삭제** (필요시)
   - iOS: 설정 → Safari → 기록 및 웹사이트 데이터 지우기
   - Android: Chrome 설정 → 개인정보 보호 → 인터넷 사용 기록 삭제

---

## 🎯 팁

### 1. 별칭 만들기
매번 긴 명령어 입력 싫으면:

```bash
# ~/.bashrc 또는 ~/.zshrc에 추가
alias update-price='cd /home/user/webapp && python3 auto_deploy.py'

# 사용
update-price
```

### 2. 스케줄링
매일 자동 실행하려면 cron 사용:

```bash
# crontab -e
0 9 * * * cd /home/user/webapp && python3 auto_deploy.py
# 매일 오전 9시 자동 실행
```

### 3. Slack/이메일 알림
배포 완료 시 알림 받기 (고급):

```bash
# auto_deploy.py 마지막에 추가
# curl -X POST -H 'Content-type: application/json' \
#   --data '{"text":"가격표 업데이트 완료!"}' \
#   YOUR_SLACK_WEBHOOK_URL
```

---

## 📞 문의

문제가 있으면 스크립트 실행 시 나오는 에러 메시지를 확인해주세요.

---

**만든 날짜**: 2026-02-12
**버전**: 1.0
**작성자**: GenSpark AI Developer
